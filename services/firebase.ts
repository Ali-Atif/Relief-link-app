/**
 * Placeholder for Firebase initialization (e.g. `initializeApp`, Auth, Firestore).
 * Add the `firebase` package and wire credentials when you implement features.
 */
export function isFirebaseConfigured(): boolean {
  return false;
}
