/** App-wide colors and layout tokens (expand as the design system grows). */
export const colors = {
  background: '#f8fafc',
  surface: '#ffffff',
  primary: '#0d9488',
  primaryDark: '#0f766e',
  text: '#0f172a',
  textMuted: '#64748b',
  border: '#e2e8f0',
} as const;

export const spacing = {
  sm: 8,
  md: 16,
  lg: 24,
} as const;
